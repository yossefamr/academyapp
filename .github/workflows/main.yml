name: Build Android APK 🚀

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code 📂
      uses: actions/checkout@v4

    - name: Setup Bun ⚙️
      uses: oven-sh/setup-bun@v1

    - name: Install Dependencies 📦
      run: bun install

    - name: Next.js Build 🧪
      run: |
        bun run build
        # هنشوف الملفات تاني بس هنركز على الروت
        ls -F

    - name: Sync Capacitor 🔄
      run: |
        bun add -d @capacitor/cli @capacitor/android
        # هنجرب نخلي الـ webDir فاضي أو بيبص على الروت لو الـ build بيطلع ملفات هنا
        echo '{"appId": "com.lycans.fightclub", "appName": "Lycans Fight Club", "webDir": ".", "bundledWebRuntime": false}' > capacitor.config.json
        bunx cap sync

    - name: Setup Java ☕
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Build APK with Gradle 🛠️
      run: |
        cd android
        ./gradlew assembleDebug

    - name: Upload APK Artifact 🎁
      uses: actions/upload-artifact@v4
      with:
        name: Lycans-Fight-Club-APK
        path: android/app/build/outputs/apk/debug/app-debug.apk
